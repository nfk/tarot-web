# environnement
export PYTHONPATH=$SRCDIR/game:$SRCDIR/global

# run all unit test
cd $SRCDIR
python -m unittest discover -v

# coverage
cd $SRCDIR
python-coverage -x `find -name test_*.py` 
python-coverage -rm

