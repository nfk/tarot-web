# python module depends unittest2 and coverage 3.5 

# environnement
SRCDIR=$(pwd)/src
export PYTHONPATH=$SRCDIR/game:$SRCDIR/global

# run all unit test
python -m unittest discover -v src

# coverage
for f in `find -name test_*.py`; do python-coverage run -a $f; done
python-coverage report -m
